name: space_telemetry_cli

description: Space Telemetry Tracker command-line application.

version: 1.0.0

publish_to: none

resolution: workspace

environment:
  sdk: ^3.8.1

dependencies:
  space_telemetry_api:
    path: ../space_telemetry_api
  terminal_colors:
    path: ../terminal_colors

dev_dependencies:
  test: ^1.25.6
  lints: ^6.0.0